services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codeanalyzer-app
    ports:
      - "5001:5000"
    environment:
      - PORT=5000
      - FLASK_ENV=production
    restart: unless-stopped
